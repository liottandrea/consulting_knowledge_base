# macOS
.DS_Store

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# KB pipeline data (confidential / generated)
sources/
processed/
vector-store/
logs/
manifest.json
manifest.json.pre-namespace.bak

# Local source directory config — paths are machine-specific
sources.json

# qmd index + downloaded GGUF models (Docker volume)
qmd-cache/

# LLM-generated wiki content — gitignored by default (keep structure + stubs)
# index.md/log.md are regenerated with real client data on every ingest —
# never track them; see wiki/index.md.example and wiki/log.md.example instead.
/wiki/*
!/wiki/Home.md
!/wiki/*.example
!/wiki/sources/
!/wiki/entities/
!/wiki/concepts/
!/wiki/engagements/
!/wiki/synthesis/
/wiki/sources/*
!/wiki/sources/.gitkeep
/wiki/entities/*
!/wiki/entities/.gitkeep
/wiki/concepts/*
!/wiki/concepts/.gitkeep
/wiki/engagements/*
!/wiki/engagements/.gitkeep
/wiki/synthesis/*
!/wiki/synthesis/.gitkeep

# ML model cache
.cache/
hf-cache/

# Corporate CA certs (local only — never commit)
certs/*.crt
certs/*.pem

# Environment files — never commit real secrets
.env
.env.*
!.env.example

# Unrelated local tooling artifact
codebase-index/

# macOS launcher app (local)
Start Consulting KB.app/
